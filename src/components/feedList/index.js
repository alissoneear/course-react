import React from 'react';
import {style} from './index.scss';

const FeedList = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

FeedList.propTypes = {

};

export default FeedList;
